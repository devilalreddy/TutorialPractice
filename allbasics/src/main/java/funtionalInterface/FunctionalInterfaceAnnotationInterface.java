package funtionalInterface;

@FunctionalInterface
public interface FunctionalInterfaceAnnotationInterface {
    int calculate(int x);

    //cannot have more than one abstract method
   // int module(int x);
}
